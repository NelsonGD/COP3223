// swap two character variables passed as reference
void char_swap(char *x, char *y);
/*
take input into arr array, n is used to take input on how many
character the user wants and then counters array will be used to
track the number of times different letters are entered.
*/ 
void getData(char arr[], int *n, int [26]);
// display the stat
void displayStat(int *c, int total_char);
// display a character array passed to this function
void displayArr(char *a, int size);
// rever the passed char array
void reverse(char a[], int size);