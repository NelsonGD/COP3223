#define MAXSTRINGS 100
#define MAXLENGTH 51

// Function sorts string alphabetically. Not case sensitive 
void selection_sort(char names[][MAXLENGTH], int n);

// Convers all elements of array to lowercase.
void to_lower(char *s);