// pre-processor directive
#include <stdio.h>

// driver function
int main(void)
{

    double price;
    int isTaxable;
    printf("Enter the price of the item: ");
    scanf("%lf", &price);

    printf("Is this taxable item? (1 = yes, 0 = no): ");
    

}